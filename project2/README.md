# Tas# Task Manager CLI

A simple command-line task manager built with Python and Docker.

## Features
- Add tasks
- List all tasks
- Mark tasks as done
- Delete tasks
- Persistent storage (JSON file)

## Requirements
- Docker

## Usage

### Build the image
```bash
docker build -t task-manager .
